

<div>
    <h1>User Claims</h1>

    <div>
        <ul>
            @foreach (var claim in User.Claims)
            {
                <li>@claim.Type : @claim.Value : @claim.ValueType</li>
            }
        </ul>
    </div>
</div>
